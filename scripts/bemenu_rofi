#!/bin/bash

## Files
# CONFIG="$HOME/.config/hypr/wofi/config"
# STYLE="$HOME/.config/hypr/wofi/style.css"
# COLORS="$HOME/.config/hypr/wofi/colors"

## Wofi Command
wofi_command="rofi -dmenu \
      --width=350 --height=380 \
      --cache-file=/dev/null \
      --hide-scroll --no-actions \
      --define=matching=fuzzy"

notification() {
  notify-send "Be Menu" "$1"
}

menu() {
  printf "0. Dictionary\n"
  printf "1. Search Man Pages\n"
  printf "2. Google Search\n"
  printf "3. Open Git Repository\n"
  printf "4. Terminal\n"
  printf "5. Run...\n"
  printf "6. Kill Process\n"
  printf "7. Focus Mode\n"
  printf "8. Wifi Connect\n"
  printf "9. Exit System\n"
}

main() {
  choice=$(menu | ${wofi_command} | cut -d. -f1)

  case $choice in
  0)
    notification "Search dictionary"
    bemenu_input -d
    return
    ;;
  1)
    notification "Search man pages"
    bemenu_input -m
    return
    ;;
  2)
    notification "Google search"
    bemenu_input -g
    return
    ;;
  3)
    notification "Open git repo"
    bemenu_input -r
    return
    ;;
  4)
    notification "Open terminal"
    bemenu_input -t
    return
    ;;
  5)
    notification "Run..."
    bemenu_input -l
    return
    ;;
  6)
    notification "Kill process"
    bemenu_input -k
    return
    ;;
  7)
    notification "Focus mode"
    bemenu_input -t
    return
    ;;
  8)
    notification "Connect to wifi"
    bemenu_input -w
    return
    ;;
  9)
    notification "Kill process"
    bemenu_input -e
    return
    ;;
  esac
}

main
